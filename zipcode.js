/*before using this module, I installed it by entering the following in command line:

npm install zipcode

which installed it into the directory in which I was working.

Follow the documentation as laid out online:
npmjs.org/package/zipcode
*/

//To use the module:
var zipcode = require('zipcode');

console.log(zipcode.lookup('55443'));